Given the above output return the answer using the following JSON schema.
```json
{{
  "type": "object",
  "properties": {{
    "gene": {{
      "type": "string"
    }},
    "is_involved": {{
      "type": "string"
    }}
  }},
}}
```
Here `gene` is the gene considered and `is_involved` is `yes` if the answer is yes otherwise it is set to `no`. Place the output in a JSON code block. Do not add any comments in the JSON code block.
